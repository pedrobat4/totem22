/**
 * Configuração POR TOTEM. Cada unidade física tem a sua — troque aqui que o
 * QR gerado passa a carregar a localização certa, e o /validador mostra ela.
 */
export const TOTEM = {
  id: 'totem-renner-01',
  location: 'Em frente à Renner',
}
